{
  "name": "patlix-world-shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/patlix-world-shared/src",
  "projectType": "library",
  "tags": ["scope:shared"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/patlix-world-shared",
        "command": "npx tsc --noEmit"
      }
    },
    "lint": {
      "executor": "nx:noop"
    },
    "test": {
      "executor": "nx:noop"
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/patlix-world-shared",
        "command": "npm run build"
      }
    }
  }
}